# Create, delete, and modify local user accounts

$ sudo useradd john
$ ls -a /etc/skel

$ useradd --defaults ==> useradd -D
$ cat /etc/login.defs

$ sudo passwd john

$ sudo userdel john

$ sudo userdel --remove john ==> sudo userdel -r john
$ sudo useradd -s /bin/othershell -d /home/otherdirectory/ john
$ sudo useradd -s /bin/othershell john

$ cat /etc/passwd

$ sudo useradd --uid 1100 smith ==> sudo useradd -u 1100 smith

$ ls -ln /home/

$ id

$ whoami

$ sudo useradd --system sysacc

$ sudo userdel -r john
$ sudo userdel -r smith

$ useradd --help

$ sudo useradd john
$ sudo usermod --home /home/otherdirectory --move-home john
$ sudo usermod -d /home/otherdirectory -m john

$ sudo usermod --login jane john ==> sudo usermod -l jane john
$ sudo usermod --shell /bin/othershell jane ==> sudo usermod -s /bin/othershell jane

$ sudo usermod --lock jane ==>  sudo usermod -L jane
$ sudo usermod --unlock jane ==> sudo usermod -U jane

$ sudo usermod --expiredate 2021-12-10 jane ==> sudo usermod -e 2021-12-10 jane

$ sudo usermod --expiredate "" jane ==> sudo usermod -e "" jane

$ sudo chage --lastday 0 jane ==> sudo change -d 0 jane
$ sudo chage --lastady -1 jane ==> sudo change -d -1 jane

$ sudo chage --maxdays 30 jane ==> sudo chage -M 30 jane
$ sudo chage --maxdays -1 jane ==> sudo chge -M -1 jane

$ sudo chage --list jane ===> sudo chage -l jane

$ sudo userdel -r jane

$ sudo groupdel john

# Create, delete, and modify local groups and group memberships

$ sudo useradd richard
$ sudo groupadd developers

$ sudo gpasswd --add richard developers ==> sudo jpasswd --a richard developers

$ groups richard

$ sudo gpasswd --delete richard developers ==> sudo gpasswd -d richard developers

$ sudo usermod -g developers richard
$ sudo usermod --gid developers richard ==> sudo usermod -G developers richard

$ groups richard

$ gpasswd --help

$ sudo groupmod --new-name programmers developers ==> sudo groupmod -n programmers developers

$ sudo groupdel programmers

$ sudo usermod --gid richard richard

$ sudo groupdel programmers

# Manage system-wide environment profiles

$ printenv ===> env
$ HISTSIZE=2000
$ history

$ echo $HOME

$ touch $HOME/saved_file

$ cat .bashrc
$ sudo vim /etc/environment
  KODEKLOUD=https://kodekloud.com/

$ logout

$ echo $KODEKLOUD

$ sudo vim /etc/profile.d/lastlogin.sh
  echo "Your last login was at: " > $HOME/lastlogin
  date >> $HOME/lastlogin

$ logout

$ ls

$ cat lastlogin

# Manage template user environment

$ sudo vim /etc/skel/README 
  Please don't run CPU-intensive processes between 8AM and 10PM

$ sudo useradd trinity
$ sudo ls -a /home/trinity

$ cat README

$ sudo vim /home/triniti/.bashrc
  PATH="$HOME/.local/bin:$HOME/bin:$PATH"
  PATH="$HOME/.local/bin:$HOME/bin:/opt/bin:$PATH"

$ echo $PATH

$ specialtool

$ sudo vim /etc/skel/.bashrc

# Configure user resource limits

$ sudo vi /etc/security/limits.conf
    <domain>    <type>    <item>    <value>
    trinity     hard      nproc     10
    @developers soft      nproc     20  

$ man limits.conf

$ sudo -iu trinity
$ ps | less

$ ls -a | grep bash | less

$ logout

$ ulimit -a

$ ulimit -u 5000

# Manage user privileges

$ groups
$ sudo gpasswd -a trinity wheel

$ sudo gpasswd -d trinity wheel
$ sudo visudo
    user/group    host=(run_as_user)    command_list
    %wheel        ALL=(ALL)             ALL
    trinity       ALL=(ALL)             ALL
    %developers   ALL=(ALL)             ALL
    trinity       ALL=(aaron, john)     ALL
    trinity       ALL=(ALL)             /bin/ls, /bin/stat
    trinity       ALL=                 /bin/ls, /bin/stat
    trinity       ALL=                 NOPASSWD:ALL


$ sudo -u trinity ls /home/trinity
$ sudo ls
$ sudo stat /bin

# Manage access to the root account

$ sudo ls /root/
$ sudo --login ==> sudo -i
$ logout
$ su - ==> su -l ==> su --login

$ sudo --login
$ sudo passwd root
$ sudo passwd --unlock root ==> sudo passwd -u root
$ su -

$ sudo passwd --lock root ==> sudo passwd -l root

# Configure PAM (Pluggable Authentication Modules)
$ su

$ ls /etc/pam.d/
$ sudo vim /etc/pam.d/su
$ man pam.conf
$ man pam_wheel